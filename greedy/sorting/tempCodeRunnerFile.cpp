int rem;
        // for(int i=0;i<arr.size();i++){
        //     rem=arr[i]%10;
        //     sc[rem].first++;
        //     sc[rem].second.push_back(arr[i]);
        // }
        // arr.clear();
        // for(int i=0;i<sc.size();i++){
        //     if(sc[i].first>0){
        //         for(int j=0;j<sc[i].second.size();i++){
        //             arr.push_back(sc[i].second.at(j));
        //         }
        //     }
        // }